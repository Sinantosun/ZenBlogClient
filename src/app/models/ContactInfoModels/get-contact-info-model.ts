import { SafeHtml } from "@angular/platform-browser";

export class GetContactInfoModel {
    id: string = "";
    address: string = "";
    email: string = "";
    phone: string = "";
    mapURL: string  = "";
}